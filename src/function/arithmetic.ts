export const subtract = (x: number, y: number) => x - y;
export const sqrt = Math.sqrt;
export const cbrt = Math.cbrt;
export const pow = Math.pow;
export const multiply = (x: number, y: number): number => x * y;
export const divide = (x: number, y: number): number => x / y;
export const add = (x: number, y: number): number => x + y;
export const abs = Math.abs;
export const ceil = Math.ceil;
export const floor = Math.floor;
export const sign = Math.sign;
export const exp = Math.exp;
export const max = Math.max;
export const min = Math.min;

export const unaryMinus = (number: number) => -1 * number;
export const unaryPlus = (number: number) => number;